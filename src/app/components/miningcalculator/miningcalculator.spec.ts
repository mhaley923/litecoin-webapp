import { TestBed, async } from '@angular/core/testing';
import { MiningCalculatorComponent } from './miningcalculator';
describe('MiningCalculatorComponent', () => {

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        MiningCalculatorComponent
      ],
    }).compileComponents();
  }));


});
