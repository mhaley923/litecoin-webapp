import { TestBed, async } from '@angular/core/testing';
import { TwitterfeedComponent } from './twitterfeed';
describe('TwitterfeedComponent', () => {

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        TwitterfeedComponent
      ],
    }).compileComponents();
  }));


});
